<%
var title = ""
if(page.archive) {
  if(page.year){
    title = page.year + (page.month ? '/' + page.month : '');
  }else title = "arquivo";
}else if(page.layout == "tags"){
  	title = "marcador"
}else if(page.layout=="category"){
	title="categoria"
}else if(page.tag){
	title=page.tag
}%>

<header class="article-header article">
  <h1 class="article-title" itemprop="name">
    <%=title%>
  </h1>
</header>
